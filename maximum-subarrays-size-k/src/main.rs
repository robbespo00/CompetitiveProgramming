/*
* Given an array and an integer k, find the maximum for each and every
* contiguous subarray of size k.
*
* INPUT: The first line of input contains an integer T denoting the number
* of test cases. The description of T test cases follows. The first line
* of each test case contains a single integer N denoting the size of array
* and the size of subarray k. The second line contains N space-separated 
* integers A1, A2, ..., A_N denoting the elements of the array.
*
* OUTPUT: print the maximum for every subarray of size k.
*
* CONSTRAINTS: 
*    1 <= T <= 200
*    1 <= N <= 100
*    1 <= k <= N
*    0 <= A[i] < 1000
*/

fn main() {
    println!("Hello, world!");
}
